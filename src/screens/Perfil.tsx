import { ScrollView, Text, VStack } from 'native-base';

export default function Perfil() {
  return (
    <ScrollView flex={1} bg={'#F4F4F4'} p={5}>
      <Text color={'black'}>Perfil</Text>
    </ScrollView>
  );
}
